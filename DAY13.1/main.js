// main.js

// Import the add function from mathUtils module
import { add } from './mathUtils.js';

// Use the add function
const num1 = 5;
const num2 = 10;
const result = add(num1, num2);

console.log(`The sum of ${num1} and ${num2} is ${result}.`);
